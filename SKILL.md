---
name: feige-cover-design
description: 非哥基于实际创作经验持续优化的中文封面工作流。适用于从选题或标题开始完成平台语义路由、生成六套视觉提示词，或把一张已确认母版适配为 3:4、4:3、1:1、5:2、16:9、2.35:1、5:1 及公众号组合图；普通比例保持原构图，5:1 才使用结构重组。
---

# Feige 封面设计工作流

执行“标题确认 → 平台语义路由 → 六概念提示词与母版确认 → 满意母版多比例适配”四阶段流程。不要在标题未确认时提前设计完整画面，也不要在普通比例转换中擅自重新设计已经满意的构图。

## 运行模式

根据用户输入进入最早尚未完成的阶段：

1. 只有主题、正文或口播稿：先确认标题。
2. 标题已确认：读取 [references/concept-routing.md](references/concept-routing.md)，判断平台数量、图标类型和关系。
3. 路由完成：生成默认 6 个真正不同的视觉事件提示词，并等待用户确认满意母版。
4. 用户已经确认满意母版：进入多比例适配；如果只要求一个新比例，只处理该比例。

人物身份参考只使用当前对话中用户主动提供或明确点名的图片。不要遍历本机相册、历史项目或旧输出替用户挑选人物图。

## 阶段一：确认标题

- 用户已经给出标题时，原样锁定；只修正确凿的品牌大小写或明显错字。
- 用户没有给出标题时，理解内容对象、受众、收益或风险，提供 5 个短标题候选并指出主推。
- 标题确认前只讨论标题，不输出完整生图提示词。

## 阶段二：平台语义路由

- 默认母版为 `3:4`；用户明确指定其他比例时只覆盖本次。
- 只根据确认标题识别必要的品牌、产品、模型或平台。需要 Logo 时优先使用官方来源，并标明来源；无法可靠确认时报告缺失，不让模型猜测。
- 根据标题判断平台是单产品、同级并列还是明确流程。双平台标题可以开放双图标关系；普通单产品标题不为凑数量堆 Logo。

## 阶段三：六概念提示词与满意母版

- 默认输出 6 份独立、可直接用于图像模型的提示词。用户指定数量时按其要求。
- 六份提示词共享标题、人物身份和品牌事实，但核心物、人物动作、镜头、标题物理身份、最大轮廓和主光必须显著不同。
- 每张只讲一个可复述的动作瞬间。大人物、大标题、大核心物或准确品牌核心共同进入第一阅读层。
- 头像型品牌图标不应被放大成与真人竞争的第二张脸。
- 非哥的个人工作流优先在 ChatGPT 网页端使用 ChatGPT Images 2.0 抽卡；公开 Skill 仍兼容其他支持参考图的图像模型。
- 若宿主环境可以直接生图，可以生成候选母版；否则交付提示词和必要的官方图标，由用户在其选择的图像工具中生成。
- 只有用户明确确认满意、采用或定稿的图片才能成为尺寸适配母版。

## 阶段四：满意母版多比例适配

只有用户明确确认母版满意、采用或定稿后才进入本阶段。

默认完整尺寸包：

- `3:4`
- `4:3`
- `1:1`
- `5:2`
- `16:9`
- `2.35:1`
- `5:1`，最终像素固定为 `2000×400`
- `1:1 + 2.35:1` 公众号组合图

跳过母版已有比例。只有用户明确要求时才增减目标比例。

### 普通比例

读取 [references/ratio-native-recomposition.md](references/ratio-native-recomposition.md)。以满意母版为唯一视觉输入，保持标题分组、人物动作、手与被持物、Logo 左右关系、遮挡层级和阅读顺序，只做必要的等比缩放、轻微位移、间距调整与低细节背景延展。

多个普通比例可以并行生成。普通比例不预先抠图、不建立分层母版，也不自动获得结构重组权限。

### 5:1 超宽比例

读取 [references/wide-5x1-recomposition.md](references/wide-5x1-recomposition.md)。直接使用满意母版，不以其他比例的生成稿为中间事实源。ImageGen 只在必要时补空背景；标题、人物、Logo 和核心物优先使用满意母版原像素图层等比重排。

需要确定性合成时，使用 `scripts/render-ratio-pack.py`。

## Review 门禁

生成前建立硬文字清单，逐字记录标题、副标题、品牌词、英文大小写、数字和符号。每个结果必须按原尺寸与母版并排检查，详细标准见 [references/quality-review.md](references/quality-review.md)。

以下任一情况均不通过：

- 错字、漏字、增字、近似字或不可辨认文字；
- 标题被拆散或换行无必要改变；
- 人物动作、手与被持物、Logo 关系被改写；
- 主要内容被缩成居中的小海报，留下大块无意义空白；
- 拥挤、贴边、误裁、重复残片、脸手异常或 Logo 变形；
- 比例、尺寸或色彩不符合要求。

通过项立即冻结。失败比例必须从满意母版重新生成，只追加一个针对失败点的短修正；不要使用失败稿继续迭代，也不要重做已通过比例。

只有 `1:1` 与 `2.35:1` 都通过后，才运行 `scripts/build-wechat-cover-stitch.py` 生成公众号组合图。未通过的比例不得进入正式总览或最终交付。

## 创作者配置

默认规则不绑定特定人物、服装和品牌。需要长期稳定的个人风格时，复制并填写 [references/creator-profile.example.md](references/creator-profile.example.md)，再由用户在项目上下文中明确指定该配置。

## 交付

- 标题阶段：候选标题与主推理由。
- 提示词阶段：必要的官方图标来源，以及相互独立的完整提示词。
- 尺寸阶段：逐比例 Review 结论、通过文件、公众号组合图和实际输出目录。
- 5:1：额外说明构图参考、色彩/内容参考、原像素图层范围和 `2000×400` 检查结果。
